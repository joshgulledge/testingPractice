/*
  if a year is divisible by 4 return its a leap year
  if a year is divisible by 100 return not a leap year
  if a year divisible by 400 it is a leap year
*/

const leapYear = function () {
  
}; // end leapYear

module.exports = leapYear;